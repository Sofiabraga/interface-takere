# Supabase — copie estes valores de Project Settings → API.
# São lidos pelo app Expo (precisam do prefixo EXPO_PUBLIC_).
# Use APENAS a anon key aqui. NUNCA coloque a service_role.
EXPO_PUBLIC_SUPABASE_URL=colocar_url
EXPO_PUBLIC_SUPABASE_ANON_KEY=colocar_key
